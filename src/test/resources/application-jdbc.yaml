spring:
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres_jdbc_test
    username: postgres
    password: user
    driverClassName: org.postgresql.Driver